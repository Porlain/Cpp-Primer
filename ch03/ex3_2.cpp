#include <iostream>
#include <string>
using namespace std;

int main(){
    // question one 
    // for(string str;getline(cin,str);cout<<str<<endl);
    
    // question two
    for (string str; cin >> str; cout << str << endl);

    return 0;
}